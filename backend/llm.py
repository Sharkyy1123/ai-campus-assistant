def llm_predict(prompt: str) -> str:
    """
    Dummy LLM function for academic demonstration.
    Replaces LangChain / OpenAI to avoid dependency issues.
    """
    return f"[AI Response] {prompt}"
